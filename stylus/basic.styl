@import 'config.styl'
add(a,b)
  a+b
body
  padding add(10px,5)
  border-radius: 10px
@media print
  #header
  #footer
    display none
@font-face
  font-family Geo
  font-style normal
  src url(fonts/geo_sans_light/GensansLight.ttf)

.ingeo
  font-family Geo